{
  "name": "emtrey",
  "version": "0.0.0",
  "scripts": {
    "start": "npm run build-ts && npm run serve",
    "serve": "node dist/app.js --url=http://localhost:3000 --dir=/Users/brad/h2/emtrey/examples/react --app=example-react",
    "build": "rm -rf ./dist && npm run build-ts",
    "test": "jest --forceExit --coverage --verbose",
    "watch-test": "npm run test -- --watchAll",
    "build-ts": "tsc",
    "watch-ts": "tsc -w",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "debug": "npm run build && npm run watch-debug"
  },
  "dependencies": {
    "form-data": "^3.0.0",
    "glob": "^7.1.6",
    "handlebars": "^4.7.6",
    "puppeteer": "^5.2.1",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/form-data": "^2.5.0",
    "@types/handlebars": "^4.1.0",
    "@types/node": "^14.0.14",
    "@types/puppeteer": "^3.0.1",
    "@types/rimraf": "^3.0.0",
    "prettier": "^2.0.5",
    "typescript": "^3.9.5"
  }
}
